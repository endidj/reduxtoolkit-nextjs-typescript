1. yarn create next-app . --typescript

2. yarn add redux react-redux @reduxjs/toolkit
   // 선택 :redux-logger(로거 미들웨어)

3. yarn add -D @types/react-redux
   // 선택 : @types/redux-logger(로거 미들웨어@types)
